{
	"name": "tunnel-ddns-worker",
	"version": "0.0.0",
	"private": true,
	"scripts": {
		"deploy": "wrangler deploy",
		"dev": "wrangler dev --test-scheduled",
		"start": "wrangler dev --test-scheduled",
		"cf-typegen": "wrangler types"
	},
	"devDependencies": {
		"@types/node": "^24.10.1",
		"typescript": "^5.5.2",
		"wrangler": "^4.51.0"
	},
	"dependencies": {
		"cloudflare": "^5.2.0"
	}
}